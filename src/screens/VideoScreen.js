import { Box, Center, Text } from "native-base";

export default function VideoScreen(props){
    return(<Box>
        <Center p={4}>
            <Text fontSize={24} color='gray.300'>No Product in Cart</Text>
        </Center>
    </Box>)
}