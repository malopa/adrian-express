import { Box, Center, Text } from "native-base";

export default function VideoScreen(props){
    return(<Box>
        <Center p={4}>
            <Text fontSize={24} color='gray.300'>You will upload Video here</Text>
        </Center>
    </Box>)
}